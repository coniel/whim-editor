{
  "name": "@sheets-editor/plugin-hovering-toolbar",
  "version": "0.12.1",
  "description": "Adds support for a hovering toolbar.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/coniel/slash.git"
  },
  "dependencies": {
    "@sheets-editor/core": "^0.12.0"
  },
  "peerDependencies": {
    "react": "^16.9.0",
    "slate": "^0.58.1",
    "slate-react": "^0.58.1"
  },
  "devDependencies": {
    "@sheets-editor/material-ui": "^0.12.1",
    "@sheets-editor/plugin-link": "^0.12.1",
    "@sheets-editor/plugin-rich-text": "^0.12.1"
  },
  "scripts": {
    "build:declaration": "tsc --project tsconfig.build.json",
    "build:es": "BABEL_ENV=build babel src --root-mode upward --out-dir dist --source-maps --extensions .ts,.tsx --delete-dir-on-start --no-comments"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ]
}
